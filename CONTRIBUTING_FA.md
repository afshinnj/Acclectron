# راهنمای مشارکت در Acclectron

از گزارش خطا، پیشنهاد قابلیت و Pull Request استقبال می‌شود.

## گزارش خطا

در Issue این موارد را بنویسید:

1. نسخهٔ برنامه و سیستم‌عامل
2. مراحل دقیق بازتولید مشکل
3. نتیجهٔ مورد انتظار و نتیجهٔ واقعی
4. پیام خطا، تصویر یا لاگ مرتبط (در صورت وجود)

اطلاعات محرمانه، فایل پایگاه‌دادهٔ واقعی و رمز عبور را در Issue یا Pull Request قرار ندهید.

## توسعهٔ محلی

```bash
npm install
npm start
```

قبل از ارسال تغییرات، این دستورات باید بدون خطا اجرا شوند:

```bash
npm run check
npm test
```

## Pull Request

- هر Pull Request یک هدف مشخص و توضیح کوتاه داشته باشد.
- تغییرات رابط کاربری را با تصویر یا توضیح مسیر کار مستند کنید.
- تغییرات پایگاه‌داده، مهاجرت و اثر آن بر داده‌های قبلی را توضیح دهید.
- تست‌های مرتبط را اضافه یا به‌روزرسانی کنید.
- از commitهای کوچک و پیام‌های روشن استفاده کنید.
